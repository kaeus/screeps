var resourceBalancer = {
    findResource: function(creep) {
        if (Memory.res)  
    },
    resourceList: function(creep) {
        var room = creep.room;
        var energySources = room.find(FIND_SOURCES);
    }
};
module.exports = resourceBalancer;